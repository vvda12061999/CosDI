'use strict';

module.exports = {
    description: 'VContainer-style dependency injection for Cocos Creator 3.0+. Import this extension, then enable it — the runtime is installed into assets/CosDI automatically.',
};
