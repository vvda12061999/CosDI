'use strict';

module.exports = {
    description: 'VContainer-style dependency injection for Cocos Creator. Import this extension, then enable it — the runtime is installed into assets/CosDI automatically.',
};
